import{THREAD_STATUSES,type ThreadStatus}from"./client/current-schema.js";export{THREAD_STATUSES,type ThreadStatus};export const SETTLED_THREAD_STATUSES=new Set<ThreadStatus>(["pending_user","error","ready_for_review","idle","archived"]);export const isThreadSettled=(x:{status:ThreadStatus})=>SETTLED_THREAD_STATUSES.has(x.status);export const isThreadDone=(x:{status:ThreadStatus})=>x.status==="ready_for_review"||x.status==="idle";export const threadUrl=(web:string,project:string,id:string)=>`${web.replace(/\/+$/,"")}/project/${encodeURIComponent(project)}/captain/${encodeURIComponent(id)}`;
