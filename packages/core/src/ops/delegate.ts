import{z}from"zod";import{resources}from"../client/resources.js";import{THREAD_STATUSES,threadUrl}from"../model.js";import{defineOp}from"./define.js";import{requireProject}from"./shared.js";export const delegate=defineOp({name:"delegate",summary:"Create and start a Capy thread.",effect:"create",input:z.object({message:z.string().min(1),requestId:z.string().min(1).max(191),projectId:z.string().optional(),title:z.string().optional(),modelId:z.string().optional(),reasoningMode:z.string().optional(),fast:z.boolean().optional(),machineSize:z.enum(["small","medium","large","ultra","hyper","bigguy"]).optional()}),output:z.object({threadId:z.string(),projectId:z.string(),title:z.string().nullable(),status:z.enum(THREAD_STATUSES),url:z.string(),createdAt:z.string()}),async run(a,c){const projectId=requireProject(a.projectId,c);const model=a.modelId?{modelId:a.modelId,...(a.reasoningMode?{reasoningMode:a.reasoningMode}:{}),...(a.fast!==undefined?{modes:{fast:a.fast}}:{})}:undefined;const t=await resources(c).threads.create({requestId:a.requestId,projectId,message:a.message,...(a.title?{title:a.title}:{}),...(model?{model}:{}),...(a.machineSize?{machineSize:a.machineSize}:{})});return{threadId:t.id,projectId:t.projectId,title:t.title,status:t.status,url:threadUrl(c.webBaseUrl,t.projectId,t.id),createdAt:t.createdAt}}});
