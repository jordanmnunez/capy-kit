import{z}from"zod";import{resources}from"../client/resources.js";import{THREAD_STATUSES}from"../model.js";import{defineOp}from"./define.js";import{requireProject}from"./shared.js";const page=z.object({items:z.array(z.unknown()),cursor:z.string().nullable()});export const threadsList=defineOp({name:"threads.list",summary:"List project threads.",effect:"read",input:z.object({projectId:z.string().optional(),status:z.enum(THREAD_STATUSES).optional(),limit:z.coerce.number().int().min(1).max(100).optional(),cursor:z.string().optional()}),output:page,run:(a,c)=>resources(c).threads.list({projectId:requireProject(a.projectId,c),status:a.status,limit:a.limit,cursor:a.cursor})});export const threadsGet=defineOp({name:"threads.get",summary:"Get a thread.",effect:"read",input:z.object({id:z.string().min(1)}),output:z.unknown(),run:(a,c)=>resources(c).threads.get(a.id)});const control=(name:string,summary:string,method:"interrupt"|"archive"|"unarchive")=>defineOp({name,summary,effect:"mutate" as const,input:z.object({id:z.string().min(1)}),output:z.unknown(),run:(a,c)=>resources(c).threads[method](a.id)});export const threadsInterrupt=control("threads.interrupt","Interrupt active thread work.","interrupt");export const threadsArchive=control("threads.archive","Archive a thread without interrupting it.","archive");export const threadsUnarchive=control("threads.unarchive","Unarchive a thread.","unarchive");export const threadsMessage=defineOp({name:"threads.message",summary:"Send a message to a thread.",effect:"create",input:z.object({id:z.string().min(1),text:z.string().min(1),delivery:z.enum(["interrupt","queue","steer"]).optional()}),output:z.object({id:z.string(),deduped:z.boolean()}),run:(a,c)=>resources(c).threads.message(a.id,{text:a.text,delivery:a.delivery})});export const threadsMessages=defineOp({name:"threads.messages",summary:"Read a thread transcript.",effect:"read",input:z.object({id:z.string().min(1),after:z.string().optional(),limit:z.coerce.number().int().min(1).max(100).optional()}),output:page,run:(a,c)=>resources(c).threads.messages(a.id,{after:a.after,limit:a.limit})});
