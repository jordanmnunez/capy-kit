# scripts/

No executable helper is checked in here today.

A future `gen-skills.ts` may derive mechanical command-reference tables from the `OPS` registry, but
it is only planned. There is currently no `scripts/gen-skills.ts`, no `npm run gen:skills`, and no CI
drift check for skill tables. The two skill manuals under `skills/` are maintained by hand.
